# 📱 إضافة ملفات الترجمة إلى Xcode

## ✅ الملفات المُنشأة

تم إنشاء ملفات الترجمة التالية:
- `ios/Runner/ar.lproj/InfoPlist.strings` (العربية)
- `ios/Runner/en.lproj/InfoPlist.strings` (الإنجليزية)

## 🔧 خطوات إضافة الملفات إلى Xcode

### **الطريقة 1: إضافة الملفات يدوياً في Xcode (موصى به)**

1. **افتح المشروع في Xcode:**
   ```bash
   cd /Users/ibraheem.designer/Documents/hosoony2/hosoony_flutter
   open ios/Runner.xcworkspace
   ```

2. **في Xcode:**
   - في Project Navigator (الشريط الجانبي الأيسر)
   - انقر بزر الماوس الأيمن على مجلد `Runner`
   - اختر **"Add Files to Runner..."**

3. **أضف المجلدات:**
   - انتقل إلى: `ios/Runner/`
   - اختر مجلد `ar.lproj`
   - تأكد من:
     - ✅ "Copy items if needed" **غير محدد** (الملفات موجودة بالفعل)
     - ✅ "Create groups" محدد
     - ✅ Target "Runner" محدد
   - اضغط "Add"
   
   - كرر نفس الخطوات لمجلد `en.lproj`

4. **تحقق من الإضافة:**
   - يجب أن تظهر المجلدات في Project Navigator:
     ```
     Runner/
       ├── ar.lproj/
       │   └── InfoPlist.strings
       └── en.lproj/
           └── InfoPlist.strings
     ```

### **الطريقة 2: التحقق من أن الملفات موجودة**

إذا كانت الملفات موجودة بالفعل في النظام، يمكنك التحقق:

```bash
# التحقق من وجود الملفات
ls -la ios/Runner/ar.lproj/
ls -la ios/Runner/en.lproj/
```

إذا كانت موجودة، قد تحتاج فقط إلى إعادة فتح Xcode أو تنظيف المشروع.

---

## ✅ التحقق من الإعداد

### **1. في Xcode:**

1. اختر ملف `Info.plist` في Project Navigator
2. تأكد من أن `CFBundleDevelopmentRegion` = `ar`
3. تأكد من وجود `CFBundleLocalizations` مع `ar` و `en`

### **2. اختبار البناء:**

```bash
cd /Users/ibraheem.designer/Documents/hosoony2/hosoony_flutter

# تنظيف المشروع
flutter clean

# الحصول على التبعيات
flutter pub get

# بناء للتأكد من عدم وجود أخطاء
flutter build ios --release
```

---

## 🔍 استكشاف الأخطاء

### **المشكلة: الملفات لا تظهر في Xcode**

**الحل:**
1. أغلق Xcode تماماً
2. افتح `Runner.xcworkspace` مرة أخرى
3. إذا لم تظهر، أضفها يدوياً باستخدام الطريقة 1

### **المشكلة: خطأ في البناء**

**الحل:**
```bash
# تنظيف شامل
flutter clean
cd ios
rm -rf Pods Podfile.lock
pod install
cd ..
flutter pub get
```

---

## 📝 ملاحظات

- ملفات `.lproj` هي المجلدات القياسية لملفات الترجمة في iOS
- `InfoPlist.strings` يستخدم لترجمة نصوص `Info.plist`
- بعد إضافة الملفات، يجب أن يعرض التطبيق الاسم العربي على أجهزة iOS العربية

---

**بعد إضافة الملفات، يمكنك متابعة إصلاح App Store Connect باستخدام:** `APP_STORE_ARABIC_METADATA_FIX.md`

