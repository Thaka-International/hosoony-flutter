# 🕌 حصوني القرآني - Flutter App (مع أدوات التصحيح)

## 📱 **نظرة عامة**
تطبيق Flutter لنظام إدارة التعلم القرآني، يوفر واجهة مستخدم حديثة ومتجاوبة للطلاب والمعلمين مع أدوات تصحيح شاملة للاختبار والتطوير.

## 🚀 **الميزات الرئيسية**

### **👨‍🎓 للطلاب:**
- 🔐 **تسجيل الدخول** بالإيميل أو الهاتف
- 📚 **المهام اليومية** ومتابعة الإنجاز
- 👥 **نظام الرفيقات** والغرف الافتراضية
- 🏆 **النقاط والشارات** وتتبع التقدم
- 📅 **الجدول الدراسي** والمتابعة
- 💳 **إدارة الاشتراكات** والمدفوعات
- 🔔 **الإشعارات** متعددة القنوات

### **👨‍🏫 للمعلمين:**
- 📊 **تقييم الأداء** والمتابعة
- 📈 **التقارير والإحصائيات**
- 👥 **إدارة الطلاب** والفصول
- 📝 **إنشاء المهام** والتقييمات

### **🔧 أدوات التطوير (جديد):**
- 🚀 **تجاوز تسجيل الدخول** لجميع أنواع المستخدمين
- 🔍 **أدوات التصحيح الشاملة** مع تسجيل مفصل
- 🌐 **مراقبة API** مع تسجيل الطلبات والاستجابات
- ⚡ **مراقبة الأداء** مع قياس أوقات التنفيذ
- 🐛 **نافذة تصحيح تفاعلية** لعرض معلومات التطبيق

---

## 🛠️ **التقنيات المستخدمة**

- **Flutter:** إطار العمل الأساسي
- **Dart:** لغة البرمجة
- **Riverpod:** إدارة الحالة
- **Dio:** طلبات HTTP مع تسجيل مفصل
- **Flutter Secure Storage:** التخزين الآمن
- **Logger:** نظام السجلات المتقدم
- **Go Router:** التوجيه والتنقل

---

## 📋 **المتطلبات**

- Flutter SDK 3.0+
- Dart 3.0+
- Android Studio / VS Code
- Android SDK (للأندرويد)
- Xcode (لـ iOS)

---

## 🚀 **التثبيت والتشغيل**

### **1. استنساخ المشروع:**
```bash
git clone https://github.com/Thaka-International/hosoony-flutter.git
cd hosoony-flutter
```

### **2. تثبيت التبعيات:**
```bash
flutter pub get
```

### **3. تشغيل التطبيق:**
```bash
# للأندرويد
flutter run

# للويب
flutter run -d chrome

# لـ iOS
flutter run -d ios
```

---

## 🔧 **أدوات التصحيح والتجاوز**

### **تجاوز تسجيل الدخول:**
1. افتح التطبيق في وضع التطوير
2. في صفحة تسجيل الدخول، اضغط على "تجاوز تسجيل الدخول"
3. اختر نوع المستخدم المطلوب:
   - 👩‍🎓 طالبة
   - 👩‍🏫 معلمة
   - 👩‍💼 مساعدة
   - 👨‍💼 مدير
   - 👨‍🔧 مدير فرعي
   - 👩‍🔧 دعم المعلمين

### **أدوات التصحيح:**
- اضغط على "أدوات التصحيح" لعرض نافذة التصحيح التفاعلية
- جميع العمليات يتم تسجيلها تلقائياً في وحدة التحكم
- مراقبة طلبات API واستجاباتها
- تتبع حالات المصادقة والتنقل

---

## 📱 **بناء التطبيق**

### **بناء APK للأندرويد (مع أدوات التصحيح):**
```bash
flutter build apk --debug
```

### **بناء APK للإنتاج:**
```bash
flutter build apk --release
```

### **بناء تطبيق الويب:**
```bash
flutter build web --release
```

### **بناء تطبيق iOS:**
```bash
flutter build ios --release
```

---

## 🔧 **التكوين**

### **ملف البيئة:**
```dart
// lib/core/config/env.dart
class Env {
  static const String baseUrl = 'https://thakaa.me/api/v1';
  static const String appName = 'حصوني القرآني';
  static const bool isDebugMode = true;
  
  // Debug settings
  static const bool enableDebugLogging = true;
  static const bool enableApiLogging = true;
  static const bool enablePerformanceLogging = true;
  static const bool enableLoginBypass = true;
}
```

### **إعدادات API:**
```dart
// lib/data/api/api_client.dart
class ApiClient {
  static const String baseUrl = 'https://thakaa.me/api/v1';
  static const Duration timeout = Duration(seconds: 30);
}
```

---

## 📁 **هيكل المشروع**

```
lib/
├── core/
│   ├── config/          # إعدادات التطبيق
│   ├── debug/           # أدوات التصحيح والتجاوز (جديد)
│   ├── errors/          # معالجة الأخطاء
│   ├── router/          # التوجيه والتنقل
│   └── theme/           # التصميم والألوان
├── data/
│   ├── api/             # طلبات API
│   └── models/          # نماذج البيانات
├── features/
│   ├── auth/            # المصادقة (مع تجاوز تسجيل الدخول)
│   ├── dashboard/       # لوحة التحكم
│   ├── tasks/           # المهام
│   ├── companions/      # الرفيقات
│   ├── payments/        # المدفوعات
│   └── profile/         # الملف الشخصي
├── services/            # الخدمات (مع تسجيل مفصل)
├── shared_ui/           # واجهات مشتركة
└── main.dart           # نقطة البداية (مع تهيئة التصحيح)
```

---

## 🔐 **المصادقة**

### **تسجيل الدخول بالإيميل:**
```dart
final authNotifier = ref.read(authStateProvider.notifier);
await authNotifier.login(email, password);
```

### **تسجيل الدخول بالهاتف:**
```dart
await authNotifier.loginWithPhone(phoneNumber);
await authNotifier.verifyPhoneCode(code);
```

### **تجاوز تسجيل الدخول (للتطوير):**
```dart
// تسجيل الدخول كطالب
await LoginBypassService.loginAsStudent();

// تسجيل الدخول كمعلم
await LoginBypassService.loginAsTeacher();
```

---

## 📚 **الاستخدام**

### **الحصول على المهام اليومية:**
```dart
final tasks = await ApiService.getDailyTasks(studentId);
```

### **تسليم سجل يومي:**
```dart
await ApiService.submitDailyLog({
  'student_id': studentId,
  'task_id': taskId,
  'status': 'completed',
  'notes': 'تم إنجاز المهمة بنجاح'
});
```

### **الحصول على الرفيقات:**
```dart
final companions = await ApiService.getMyCompanions();
```

---

## 🧪 **الاختبار**

### **تشغيل الاختبارات:**
```bash
flutter test
```

### **اختبار الواجهات:**
```bash
flutter test test/widget_test.dart
```

### **اختبار مع أدوات التصحيح:**
- استخدم تجاوز تسجيل الدخول لاختبار جميع الصفحات
- راقب سجلات التصحيح في وحدة التحكم
- استخدم نافذة التصحيح التفاعلية

---

## 📊 **الأداء**

### **تحليل الأداء:**
```bash
flutter run --profile
```

### **تحليل الحجم:**
```bash
flutter build apk --analyze-size
```

### **مراقبة الأداء مع التصحيح:**
- جميع العمليات يتم تسجيل أوقاتها تلقائياً
- مراقبة طلبات API وأوقات الاستجابة
- تتبع استخدام الذاكرة والمعالج

---

## 🐛 **استكشاف الأخطاء**

### **مشاكل شائعة:**

1. **خطأ في الاتصال:**
   - تحقق من إعدادات الشبكة
   - تأكد من صحة URL الأساسي
   - راقب سجلات API في وحدة التحكم

2. **مشاكل المصادقة:**
   - تحقق من صحة التوكن
   - تأكد من انتهاء صلاحية الجلسة
   - استخدم تجاوز تسجيل الدخول للاختبار

3. **مشاكل البناء:**
   - امسح الكاش: `flutter clean`
   - أعد تثبيت التبعيات: `flutter pub get`

### **أدوات التصحيح:**
- استخدم `DebugService` لتسجيل المعلومات
- راقب سجلات API في وحدة التحكم
- استخدم نافذة التصحيح التفاعلية

---

## 📈 **التطوير المستقبلي**

### **ميزات مخططة:**
- 📱 **تطبيق موبايل محسن**
- 🌐 **واجهة ويب متقدمة**
- 🔔 **إشعارات فورية**
- 📊 **تحليلات متقدمة**
- 🎮 **نظام الألعاب التعليمية**

### **تحسينات التصحيح:**
- 📊 **لوحة تحكم التصحيح** في الويب
- 📱 **إشعارات التصحيح** في التطبيق
- 🔍 **بحث في السجلات** المتقدم
- 📈 **رسوم بيانية للأداء**

---

## 🤝 **المساهمة**

### **كيفية المساهمة:**
1. Fork المشروع
2. أنشئ فرع للميزة الجديدة
3. اعمل التغييرات مع استخدام أدوات التصحيح
4. اكتب اختبارات شاملة
5. أرسل Pull Request

### **معايير الكود:**
- استخدم Dart formatting
- اكتب تعليقات واضحة
- استخدم أدوات التصحيح عند الحاجة
- اتبع مبادئ Clean Architecture

---

## 📞 **الدعم**

### **للحصول على المساعدة:**
- 📧 **البريد الإلكتروني:** dev@hosoony.com
- 🌐 **الموقع الإلكتروني:** https://thakaa.me
- 📱 **الهاتف:** +966 54 135 5804

### **للتقارير والأخطاء:**
- 🐛 **GitHub Issues:** متاح للمطورين
- 📧 **البريد الإلكتروني:** bugs@hosoony.com
- 🔧 **استخدم أدوات التصحيح** لتوفير معلومات مفصلة

---

## 📄 **الترخيص**

هذا المشروع مرخص تحت رخصة MIT. راجع ملف `LICENSE` للتفاصيل.

---

## 🎉 **شكر وتقدير**

شكر خاص لجميع المطورين والمساهمين في هذا المشروع.

---

**تم التطوير بواسطة:** Development Team  
**لأكاديمية ذكاء للتدريب** 🎓  
**© 2025 Thaka International** 🌟

---

## 🔧 **ملاحظات التطوير**

### **أدوات التصحيح متاحة فقط في وضع التطوير:**
- تجاوز تسجيل الدخول يعمل فقط مع `kDebugMode = true`
- جميع أدوات التصحيح معطلة تلقائياً في الإنتاج
- البيانات المستخدمة في الاختبار هي بيانات تجريبية

### **للبناء للإنتاج:**
```bash
# تأكد من تعطيل أدوات التصحيح
flutter build apk --release
```

**استمتع بالتطوير والاختبار!** 🚀📱✨











