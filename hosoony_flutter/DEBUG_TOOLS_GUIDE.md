# 🔧 دليل أدوات التصحيح والتجاوز

## 📱 **نظرة عامة**
تم إضافة نظام شامل للتصحيح وتجاوز تسجيل الدخول لتطبيق Flutter لتمكين اختبار جميع الصفحات والميزات بسهولة.

---

## 🚀 **ميزات تجاوز تسجيل الدخول**

### **أنواع المستخدمين المتاحة:**
- 👩‍🎓 **طالبة** - للوصول إلى صفحات الطلاب
- 👩‍🏫 **معلمة** - للوصول إلى صفحات المعلمين
- 👩‍💼 **مساعدة** - للوصول إلى صفحات المساعدة
- 👨‍💼 **مدير** - للوصول إلى صفحات الإدارة
- 👨‍🔧 **مدير فرعي** - للوصول إلى صفحات الإدارة الفرعية
- 👩‍🔧 **دعم المعلمين** - للوصول إلى صفحات دعم المعلمين

### **كيفية الاستخدام:**
1. افتح التطبيق في وضع التطوير
2. في صفحة تسجيل الدخول، اضغط على "تجاوز تسجيل الدخول"
3. اختر نوع المستخدم المطلوب
4. اضغط على زر تسجيل الدخول المناسب
5. سيتم توجيهك تلقائياً إلى الصفحة المناسبة

---

## 🔍 **أدوات التصحيح**

### **خدمة التصحيح (DebugService):**
- 📝 **تسجيل المعلومات** - تسجيل العمليات العادية
- ⚠️ **تسجيل التحذيرات** - تسجيل المشاكل المحتملة
- ❌ **تسجيل الأخطاء** - تسجيل الأخطاء مع التفاصيل
- ✅ **تسجيل النجاح** - تسجيل العمليات الناجحة
- 🌐 **تسجيل API** - تسجيل طلبات واستجابات API
- 🔐 **تسجيل المصادقة** - تسجيل حالات المصادقة
- 🧭 **تسجيل التنقل** - تسجيل انتقالات الصفحات
- ⚡ **تسجيل الأداء** - تسجيل أوقات تنفيذ العمليات

### **نافذة التصحيح التفاعلية:**
- عرض معلومات التطبيق
- عرض حالة الشبكة
- عرض حالة المصادقة
- إعادة تشغيل التطبيق

---

## 🛠️ **الملفات المضافة/المحدثة**

### **ملفات جديدة:**
- `lib/core/debug/debug_service.dart` - خدمة التصحيح الشاملة
- `lib/core/debug/login_bypass_service.dart` - خدمة تجاوز تسجيل الدخول
- `lib/features/auth/pages/login_bypass_page.dart` - صفحة تجاوز تسجيل الدخول

### **ملفات محدثة:**
- `lib/main.dart` - تهيئة خدمات التصحيح
- `lib/core/config/env.dart` - إعدادات التصحيح
- `lib/services/api_service.dart` - إضافة تسجيل API
- `lib/services/auth_service.dart` - إضافة تسجيل المصادقة
- `lib/features/auth/pages/login_page.dart` - إضافة أزرار التطوير

---

## 📋 **كيفية الاستخدام**

### **1. تفعيل وضع التطوير:**
```dart
// في lib/core/config/env.dart
static const bool isDebugMode = true;
static const bool enableLoginBypass = true;
```

### **2. استخدام تجاوز تسجيل الدخول:**
```dart
// تسجيل الدخول كطالب
await LoginBypassService.loginAsStudent();

// تسجيل الدخول كمعلم
await LoginBypassService.loginAsTeacher();

// تسجيل الدخول كمدير
await LoginBypassService.loginAsAdmin();
```

### **3. استخدام أدوات التصحيح:**
```dart
// تسجيل معلومات
DebugService.info('رسالة معلومات', 'TAG');

// تسجيل خطأ
DebugService.error('رسالة خطأ', error, stackTrace, 'TAG');

// تسجيل API
DebugService.apiRequest('POST', '/api/login', data);
DebugService.apiResponse('POST', '/api/login', 200, response);
```

---

## 🎯 **الفوائد**

### **للمطورين:**
- اختبار سريع لجميع الصفحات
- تسجيل شامل للأخطاء
- فهم أفضل لتدفق التطبيق
- توفير الوقت في الاختبار

### **للفريق:**
- اختبار أسهل للميزات الجديدة
- تقليل الأخطاء في الإنتاج
- توثيق أفضل للمشاكل
- تطوير أسرع

### **للمشروع:**
- جودة أفضل للكود
- استقرار أكبر للتطبيق
- صيانة أسهل
- تجربة مستخدم محسنة

---

## 🔧 **إعدادات التصحيح**

### **في ملف env.dart:**
```dart
// Debug settings
static const bool enableDebugLogging = true;
static const bool enableApiLogging = true;
static const bool enablePerformanceLogging = true;
static const bool enableLoginBypass = true;
```

### **تفعيل/إلغاء التصحيح:**
```dart
// تفعيل التصحيح
DebugService.setDebugMode(true);

// إلغاء التصحيح
DebugService.setDebugMode(false);

// تفعيل تجاوز تسجيل الدخول
LoginBypassService.setBypassEnabled(true);
```

---

## 📱 **بناء APK مع أدوات التصحيح**

### **للبناء مع أدوات التصحيح:**
```bash
flutter build apk --debug
```

### **للبناء بدون أدوات التصحيح:**
```bash
flutter build apk --release
```

### **ملاحظة مهمة:**
- أدوات التصحيح تعمل فقط في وضع التطوير (`kDebugMode`)
- في الإنتاج، ستكون هذه الأدوات معطلة تلقائياً
- البيانات المستخدمة في تجاوز تسجيل الدخول هي بيانات تجريبية

---

## 🚨 **تحذيرات أمنية**

### **⚠️ مهم:**
- لا تستخدم تجاوز تسجيل الدخول في الإنتاج
- تأكد من تعطيل أدوات التصحيح في الإنتاج
- لا تشارك APK مع أدوات التصحيح مع المستخدمين النهائيين
- استخدم بيانات تجريبية فقط في الاختبار

---

## 📞 **الدعم**

### **للمساعدة:**
- راجع ملفات التصحيح في `lib/core/debug/`
- تحقق من إعدادات التصحيح في `lib/core/config/env.dart`
- استخدم نافذة التصحيح التفاعلية للمعلومات

---

**🎉 استمتع بالاختبار والتطوير مع أدوات التصحيح الجديدة!**











