# صفحة الإعدادات - ملخص الميزات

## نظرة عامة
تمت إضافة صفحة إعدادات شاملة للتطبيق تتضمن خيارات التنبيهات والإشعارات الصوتية.

## الميزات المضافة

### 1. صفحة الإعدادات (`settings_page.dart`)
صفحة إعدادات كاملة مع واجهة مستخدم عربية جميلة تتضمن:

#### أقسام الصفحة:
- **رأس الصفحة** مع أيقونة وتصميم متدرج
- **قسم الإشعارات والتنبيهات**
- **قسم الإشعارات الصوتية**  
- **قسم الإجراءات** (إعادة الإعدادات، حول التطبيق)

### 2. إعدادات التنبيهات

#### ⏰ تنبيه قبل بداية الحلقة
- **الوظيفة**: إشعار قبل بداية الحلقة بـ X دقائق
- **القيمة الافتراضية**: 10 دقائق
- **خيارات الوقت المتاحة**: 10، 15، 20، 30 دقيقة
- **واجهة المستخدم**: Switch لتفعيل/تعطيل التنبيه + ChoiceChips لاختيار الوقت

```dart
// خيارات الوقت المتاحة
[10, 15, 20, 30].map((minutes) => ChoiceChip(...))
```

### 3. الإشعارات الصوتية

#### 🔊 صوت عند إشعار النشاط
- **الوظيفة**: تشغيل صوت عند استلام إشعار نشاط جديد
- **الأيقونة**: Icons.notification_important
- **القيمة الافتراضية**: مفعّل

#### 🎓 صوت عند إشعار الاختبار  
- **الوظيفة**: تشغيل صوت عند استلام إشعار اختبار
- **الأيقونة**: Icons.quiz
- **القيمة الافتراضية**: مفعّل

### 4. خزن البيانات (`settings_service.dart`)
- **Service**: `SettingsService`
- **Storage**: `SharedPreferences` للمحلية
- **Security**: `FlutterSecureStorage` للبيانات الحساسة

#### الوظائف المتاحة:
```dart
- getSettings()       // تحميل الإعدادات
- saveSettings()      // حفظ الإعدادات
- updateSetting()     // تحديث إعداد واحد
- resetToDefaults()   // إعادة الإعدادات الافتراضية
```

### 5. نموذج البيانات (`settings_model.dart`)
```dart
class AppSettings {
  final bool reminderBeforeClass;    // تنبيه قبل الحلقة
  final bool soundOnActivity;        // صوت عند النشاط
  final bool soundOnExam;             // صوت عند الاختبار
  final int reminderMinutes;         // دقائق قبل الحلقة
}
```

## التصميم

### الألوان المستخدمة:
- **رئيسي**: `AppTokens.primaryBrown`
- **نجاح**: `AppTokens.primaryGreen`
- **خلفية**: `AppTokens.neutralLight`
- **ظلال**: `AppTokens.shadowLG`

### الصور والأيقونات:
- ⚙️ أيقونة الإعدادات الرئيسية
- ⏰ أيقونة التنبيه
- 🔊 أيقونة الصوت
- 📢 أيقونة إشعار النشاط
- 📝 أيقونة الاختبار

### التنسيق:
- **Cards** للعناصر
- **SwitchListTile** للمفاتيح
- **ChoiceChips** لاختيار الوقت
- **Gradient Header** للرأس
- **Snackbars** للتأكيدات

## المسارات (Routes)

### Navigation Update:
```dart
// تمت إضافة route جديد في app_router.dart
GoRoute(
  path: 'settings',
  name: 'student-settings',
  builder: (context, state) => const SettingsPage(),
),
```

### الوصول للصفحة:
- **من صفحة الطالب الرئيسية**: زر الإعدادات ⚙️
- **URL**: `/student/home/settings`

## التفاعل مع المستخدم

### 1. حفظ الفوري
عند تغيير أي إعداد، يتم الحفظ فوراً مع رسالة تأكيد:
```dart
ScaffoldMessenger.of(context).showSnackBar(
  SnackBar(content: Text('تم حفظ الإعدادات')),
);
```

### 2. حوارات التأكيد
- **إعادة الإعدادات**: حوار تأكيد قبل التنفيذ
- **حول التطبيق**: معلومات التطبيق في حوار

### 3. ردود الفعل البصرية
- Animation للعناصر
- Transition Smooth
- Feedback فوري عند التغيير

## الدمج مع المكونات الموجودة

### التكامل مع Student Home:
```dart
// تمت إضافة زر الإعدادات في AppBar
IconButton(
  icon: const Icon(Icons.settings),
  onPressed: () => context.go('/student/home/settings'),
),
```

### الـ Copyright Widget:
تم استخدام نفس الـ widget الموجود في صفحات أخرى:
```dart
bottomSheet: const CopyrightWidget(),
```

## الملفات المضافة

1. **`lib/features/settings/models/settings_model.dart`**
   - نموذج بيانات الإعدادات

2. **`lib/features/settings/services/settings_service.dart`**
   - الخدمة لإدارة الإعدادات

3. **`lib/features/settings/pages/settings_page.dart`**
   - واجهة المستخدم الكاملة

4. **`lib/core/router/app_router.dart`** (محدث)
   - إضافة route جديد

5. **`lib/features/student/pages/student_home_page.dart`** (محدث)
   - إضافة زر الإعدادات

## الحالة الافتراضية

```dart
AppSettings(
  reminderBeforeClass: true,    // مفعّل
  soundOnActivity: true,        // مفعّل
  soundOnExam: true,            // مفعّل
  reminderMinutes: 10,          // 10 دقائق
);
```

## الخطوات التالية (Future Implementation)

للتنفيذ الكامل، ستحتاج:

1. **إشعارات وقتية** (Local Notifications)
   - استخدام `flutter_local_notifications`
   - جدولة الإشعارات قبل الحلقة

2. **أصوات التنبيه**
   - إضافة ملفات صوتية
   - استخدام `audioplayers` أو `just_audio`

3. **تحديثات الحلقة من الـ API**
   - جلب مواعيد الحلقات
   - حساب الوقت قبل بداية الحلقة

## الفوائد

✅ **تجربة مستخدم محسّنة** - تحكم كامل في الإشعارات  
✅ **تصميم عربي أصيل** - واجهة مستخدم احترافية  
✅ **خزن محلي** - لا حاجة لاتصال دائم  
✅ **سهولة الاستخدام** - واجهة بديهية  
✅ **قابلية التوسع** - يمكن إضافة المزيد من الإعدادات

## الخلاصة

تم بنجاح إضافة صفحة إعدادات كاملة مع:
- ✅ تنبيه قبل الحلقة (10-30 دقيقة)
- ✅ إشعارات صوتية للنشاطات
- ✅ إشعارات صوتية للاختبارات
- ✅ واجهة عربية جميلة
- ✅ حفظ تلقائي
- ✅ التكامل مع التطبيق

**جاهز للاستخدام!** 🎉



