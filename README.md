# Hosoony Project Structure

هذا المستودع يحتوي على البنية المنفصلة لمشروع حصوني القرآني كما هو مطلوب.

## البنية النهائية

```
hosoony2/
├── hosoony-backend/          # مستودع الباك-إند (Laravel/Filament/API)
│   ├── app/                  # كود Laravel
│   ├── config/               # ملفات التكوين
│   ├── database/             # Migrations, Seeders
│   ├── public/               # الملفات العامة
│   │   └── openapi.yaml      # وثائق API
│   ├── routes/               # مسارات التطبيق
│   ├── README.md             # توثيق الباك-إند
│   ├── Makefile              # أوامر الباك-إند
│   └── .gitignore            # ملفات مستبعدة من Git
│
├── hosoony-flutter/          # مستودع تطبيق Flutter
│   ├── lib/                  # كود Flutter
│   │   ├── core/             # الأساسيات المشتركة
│   │   ├── data/             # طبقة البيانات
│   │   ├── shared_ui/        # مكونات واجهة مشتركة
│   │   ├── features/         # الميزات حسب الدور
│   │   └── services/         # الخدمات المساعدة
│   ├── assets/               # الأصول (صور، خطوط، ترجمات)
│   ├── README.md             # توثيق Flutter
│   ├── Makefile              # أوامر Flutter
│   └── pubspec.yaml          # تبعيات Flutter
│
├── hosoony-dart-sdk/         # مستودع Dart SDK (اختياري)
│   ├── lib/                  # كود SDK
│   ├── test/                 # اختبارات SDK
│   ├── README.md             # توثيق SDK
│   └── pubspec.yaml          # تبعيات SDK
│
└── README.md                 # هذا الملف
```

## استراتيجية النشر

### 1. الباك-إند (hosoony-backend)
- **النشر**: على cPanel عبر Git
- **المحتوى**: Laravel/Filament/API فقط
- **الوصول**: عبر API endpoints
- **التوثيق**: OpenAPI في `/public/openapi.yaml`

### 2. تطبيق Flutter (hosoony-flutter)
- **النشر**: للمتاجر (Google Play, App Store)
- **المحتوى**: تطبيق الجوال فقط
- **البناء**: عبر CI/CD (GitHub Actions, Fastlane)
- **التوزيع**: APK/IPA للمتاجر

### 3. Dart SDK (hosoony-dart-sdk)
- **النشر**: مستودع Git منفصل
- **المحتوى**: SDK مولّد من OpenAPI
- **الاستخدام**: كـ dependency في Flutter
- **التحديث**: عند تغيير OpenAPI

## خطوات الإعداد

### 1. إعداد الباك-إند

```bash
cd hosoony-backend

# إعداد Git
git init
git add .
git commit -m "Initial backend setup"

# ربط بـ cPanel Git
git remote add origin https://your-cpanel-git-repo.git
git push -u origin main
```

### 2. إعداد Flutter

```bash
cd hosoony-flutter

# تثبيت التبعيات
flutter pub get

# تشغيل الكود المولد
flutter packages pub run build_runner build

# اختبار التطبيق
flutter run
```

### 3. إعداد SDK

```bash
cd hosoony-dart-sdk

# إعداد Git
git init
git add .
git commit -m "Initial SDK setup"

# ربط بالمستودع
git remote add origin https://github.com/hosoony/hosoony-dart-sdk.git
git push -u origin main
```

## الميزات الرئيسية

### 🎯 Mono-App متعدد الأدوار
- تطبيق Flutter واحد يدعم جميع الأدوار
- واجهة ديناميكية حسب دور المستخدم
- تنقل سلس بين الأدوار

### 🔄 State Management
- **Riverpod** لإدارة الحالة
- **Freezed** للكلاسات غير القابلة للتغيير
- **JsonSerializable** لتحويل JSON

### 📱 Offline-First
- **Hive** للتخزين المحلي
- مزامنة خلفية مع إعادة المحاولة
- دعم العمل بدون إنترنت

### 🌍 Internationalization
- العربية كلغة افتراضية
- الإنجليزية مدعومة
- دعم RTL كامل

### 🔔 Push Notifications
- **Firebase Cloud Messaging**
- Deep links للتنقل السريع
- إشعارات محلية

## الأدوار المدعومة

### الطالبة (Student)
- عرض المهام اليومية
- رؤية الرفيقات
- متابعة الأداء والتقييمات
- تسجيل الحضور
- رفع الإثباتات

### المعلمة (Teacher)
- إدارة الجلسات اليومية
- تقييم الطالبات
- توليد وإدارة الرفيقات
- متابعة الحضور
- إرسال الإشعارات

### الدعم الفني (Support)
- لوحة تحكم شاملة
- إدارة المستخدمين
- عرض التقارير والإحصائيات
- إعدادات النظام

## التطوير والنشر

### الباك-إند
```bash
cd hosoony-backend
make install    # تثبيت التبعيات
make serve      # تشغيل خادم التطوير
make test       # تشغيل الاختبارات
make deploy-prep # إعداد للنشر
```

### Flutter
```bash
cd hosoony-flutter
make install    # تثبيت التبعيات
make run        # تشغيل التطبيق
make test       # تشغيل الاختبارات
make build-all  # بناء جميع المنصات
```

### SDK
```bash
cd hosoony-dart-sdk
make generate   # إعادة توليد SDK
make test       # تشغيل الاختبارات
```

## الأمان

- جميع الطلبات محمية بـ Bearer Token
- تشفير البيانات الحساسة محلياً
- التحقق من صحة البيانات
- حماية من هجمات CSRF

## الأداء

- تحميل كسول للصفحات
- تخزين مؤقت للصور
- ضغط البيانات
- تحسين استهلاك البطارية

## الدعم والمساهمة

### الإبلاغ عن المشاكل
استخدم GitHub Issues للإبلاغ عن المشاكل أو طلب ميزات جديدة.

### المساهمة
1. Fork المشروع المناسب
2. إنشاء فرع للميزة الجديدة
3. Commit التغييرات
4. Push للفرع
5. إنشاء Pull Request

## الترخيص

هذا المشروع مرخص تحت رخصة MIT - راجع ملف [LICENSE](LICENSE) للتفاصيل.

## فريق التطوير

- **المطور الرئيسي**: فريق حصوني
- **التصميم**: فريق التصميم
- **الاختبار**: فريق ضمان الجودة

## التحديثات القادمة

- [ ] دعم المزيد من اللغات
- [ ] تحسينات الأداء
- [ ] ميزات جديدة للتعلم
- [ ] دعم الواقع المعزز
- [ ] تحليلات متقدمة

---

**حصوني القرآني** - نحو تعلم قرآني أفضل 📚✨