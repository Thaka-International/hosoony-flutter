# موقع وعرض سجلات تسليم المهام اليومية في Filament

## 📍 الموقع في Filament Admin Panel

**المسار:** `/admin/daily-logs`
**اسم القائمة:** `السجلات اليومية` (Daily Logs)

## 📋 جدول السجلات اليومية (DailyLogResource)

### الأعمدة المعروضة:

1. **student_id** - معرف الطالب (رقم)
2. **log_date** - تاريخ السجل
3. **status** - الحالة:
   - `pending` - معلق
   - `submitted` - تم التسليم
   - `verified` - متحقق منه
   - `rejected` - مرفوض
4. **verified_by** - معرف المستخدم الذي تحقق من السجل
5. **verified_at** - تاريخ/وقت التحقق
6. **finish_order** - ترتيب الإنجاز (للتنافس)
7. **created_at** - تاريخ الإنشاء (مخفي افتراضياً)
8. **updated_at** - تاريخ التحديث (مخفي افتراضياً)

### الفلاتر المتاحة:

1. **الطالب (student_id)** - فلتر حسب الطالب
2. **الحالة (status)** - فلتر حسب الحالة:
   - `pending` - معلق
   - `verified` - متحقق
   - `rejected` - مرفوض
3. **متحقق** - عرض السجلات المتحقق منها فقط
4. **معلق** - عرض السجلات المعلقة فقط
5. **سجل اليوم** - عرض سجلات اليوم فقط

### الإجراءات المتاحة:

#### على مستوى السجل الفردي:
1. **تعديل (Edit)** - تعديل السجل
2. **تحقق (Verify)** - تحقق من السجل:
   - يغير الحالة إلى `verified`
   - يضع `verified_by` = المستخدم الحالي
   - يضع `verified_at` = الآن
   - يظهر فقط إذا كانت الحالة ليست `verified`
3. **إعادة فتح (Reopen)** - إعادة فتح السجل:
   - يغير الحالة إلى `pending`
   - يمسح `verified_by` و `verified_at`
   - يظهر فقط إذا كانت الحالة `verified`

#### على مستوى المجموعة (Bulk Actions):
1. **حذف المحدد (Delete)**
2. **تحقق من المحدد** - تحقق من عدة سجلات دفعة واحدة
3. **إعادة فتح المحدد** - إعادة فتح عدة سجلات دفعة واحدة

### الإجراءات الإضافية:

- **تصدير (Export)** - تصدير البيانات

### الترتيب الافتراضي:

- حسب `log_date` تنازلياً (الأحدث أولاً)

## 📝 تفاصيل السجل

### عند فتح سجل محدد:

يمكنك رؤية:
- بيانات السجل الأساسية
- **ملاحظة:** حالياً **لا يوجد RelationManager** لعرض `DailyLogItem` داخل السجل

## 🔍 الجداول المستخدمة:

### 1. `daily_logs` (السجل الرئيسي)
- **الأعمدة:**
  - `id`
  - `student_id` (FK → users.id)
  - `log_date` (تاريخ السجل)
  - `status` (pending/submitted/verified/rejected)
  - `verified_by` (FK → users.id) - المستخدم الذي تحقق
  - `verified_at` (timestamp) - وقت التحقق
  - `finish_order` (integer) - ترتيب الإنجاز
  - `notes` (text) - ملاحظات
  - `created_at`, `updated_at`

### 2. `daily_log_items` (عناصر المهام في السجل)
- **الأعمدة:**
  - `id`
  - `daily_log_id` (FK → daily_logs.id)
  - `task_definition_id` (FK → daily_task_definitions.id)
  - `status` (pending/in_progress/completed/skipped)
  - `proof_type` (none/note/audio/video)
  - `quantity` (integer)
  - `duration_minutes` (integer)
  - `notes` (text)
  - `created_at`, `updated_at`

## ⚠️ ملاحظة مهمة:

**حالياً لا يوجد RelationManager لعرض `DailyLogItem` داخل صفحة `DailyLog` في Filament.**

هذا يعني أن:
- ✅ يمكنك رؤية السجلات اليومية (`daily_logs`) في الجدول
- ✅ يمكنك رؤية تفاصيل السجل الأساسية
- ❌ **لا يمكنك رؤية تفاصيل المهام الفردية (`daily_log_items`) داخل السجل مباشرة في Filament**

## 💡 تحسينات مقترحة:

### 1. إضافة RelationManager لعرض DailyLogItems:

يمكن إضافة `DailyLogItemsRelationManager` في `DailyLogResource` لعرض:
- قائمة المهام في السجل
- حالة كل مهمة (completed/pending/skipped)
- نوع الإثبات (proof_type)
- الملاحظات والكمية والمدة

### 2. تحسين عرض البيانات:

- إضافة عمود `student.name` بدلاً من `student_id`
- إضافة عمود لعرض عدد المهام المكتملة
- إضافة عمود لعرض النقاط المكتسبة

## 🔗 كيفية الوصول:

1. سجل الدخول إلى Filament Admin Panel: `https://thakaa.me/admin`
2. من القائمة الجانبية، اختر **"السجلات اليومية"**
3. سترى جدول بجميع السجلات اليومية
4. يمكنك:
   - تصفية حسب الطالب أو الحالة
   - فتح سجل محدد للتحقق أو التعديل
   - التحقق من عدة سجلات دفعة واحدة

## 📊 مثال على السجل:

عندما يسلم الطالب المهام عبر التطبيق:
1. يتم إنشاء سجل في `daily_logs` بحالة `submitted`
2. يتم إنشاء سجلات في `daily_log_items` لكل مهمة
3. يتم حساب `finish_order` (ترتيب الإنجاز)
4. يتم منح النقاط عبر `gamification_points`

## 🎯 الخطوات التالية:

لتحسين عرض السجلات في Filament، يمكن:
1. إضافة RelationManager لعرض المهام داخل السجل
2. تحسين أعمدة الجدول لعرض معلومات أكثر وضوحاً
3. إضافة تقارير وتحليلات للسجلات



